{% extends 'base.html.twig' %}

{% block backLink %}
    {{ path('home') }}
{% endblock %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}

    <div class="container">

        <div class="row mt-3">

            <div class="col-sm-8">

                <h1>Welkom {{ app.user.firstname }}</h1>

                <h3><i class="far fa-id-card"></i> Account informatie</h3>

                {{ form_start(formProfile) }}
                {{ form_widget(formProfile) }}
                <button type="submit" class="btn btn-outline-warning w-100">Wijzig</button>
                {{ form_end(formProfile) }}

            </div>

            <div class="col">

                {% for message in app.flashes('pwError') %}
                    <div class="alert alert-danger alert-dismissible fade show mt-2" role="alert">
                        {{ message }}
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                {% endfor %}
                {% for message in app.flashes('pwSuccess') %}
                    <div class="alert alert-success alert-dismissible fade show mt-2" role="alert">
                        {{ message }}
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                {% endfor %}

                <h3 class="text-center mb-4"><i class="fas fa-user-edit"></i> Wachtwoord wijzigen</h3>

                <div class="rounded border p-3">

                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-outline-warning w-100">Wijzig</button>
                    {{ form_end(form) }}
                </div>
            </div>

        </div>

    </div>

{% endblock %}